<div class="container">
  <% @articles.each do |article| %>
    <div class="my-3">
      <h3><%= link_to article.title, article_path(article) %></h3>
      <%= article.content %>
    </div>
  <% end %>